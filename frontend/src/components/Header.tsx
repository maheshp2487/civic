// Header is no longer rendered globally — each page manages its own navigation.
// The landing page has an inline nav, and the case workspace has the dark sidebar.
// This file is kept as a stub to avoid any stray imports.
export function Header() {
  return null;
}
